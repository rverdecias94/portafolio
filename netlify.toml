# Sitio estático: publicar public/ sin build ni post-procesamiento pesado.
[build]
  publish = "public"

[build.environment]
  # Sin package.json: evita instalar Python/Ruby/Go/Node en noble-new-builds.
  NETLIFY_SKIP_DEPENDENCY_INSTALL = "true"

[build.processing]
  skip_processing = true
